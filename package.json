{
  "name": "nodejs-challenge",
  "engines": {
    "node": "^24"
  },
  "version": "1.0.0",
  "devDependencies": {
    "@types/node": "24.12.0",
    "typescript": "5.9.3"
  },
  "scripts": {
    "start": "ts-node source/index.ts",
    "build": "tsc",
    "test": "echo \"Error: no test specified\" && exit 0"
  }
}
